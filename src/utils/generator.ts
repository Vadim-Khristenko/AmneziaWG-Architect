/**
 * AmneziaWG Architect — Generator compatibility shim.
 *
 * The generator implementation has moved to the `src/utils/generator/`
 * directory. This file re-exports the public API so existing imports keep
 * working without changes.
 */

export * from "./generator/index";
