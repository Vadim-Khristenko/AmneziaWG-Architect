/**
 * Re-export of the engine contract, which now lives in `types/`.
 *
 * Kept so existing imports keep working while the tree settles. New code
 * should import from `@/types/engine` directly.
 */
export * from "@/types/engine";
